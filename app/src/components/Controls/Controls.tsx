import styled from "styled-components";

export const ControlsWrapper = styled.div`
  display: flex;
  flex-direction: column;

  position: absolute;
  left: 50vw;
  top: 50vh;
`;

export const Controls = styled.select`
  display: flex;
`;

export const Button = styled.button``;
